#the general usage of any assamble software

#full commands for black-box software
#initial command of assemble software
initial_option:
  name: stringtie
  input_option: ' '
  output_option: ' -o '
  #software specific command
  additional_option: ''


#parameters for choosed software
#for catagory parameters, usage TF: 0 False 1 True / turn_on: 0 not use parameter 1 turn on parameter
parameter_bounds:
  - f: {prefix: ' -f', type: float, min: 0, max: 1, default: 0.01, hard_min: 0, hard_max: 1, step: 0.1}
  - m: {prefix: ' -m', type: int, min: 30, max: 2000, default: 200, hard_min: 30, hard_max: 'inf', step: 2000}
  - a: {prefix: ' -a', type: int, min: 0, max: 100, default: 10, hard_min: 0, hard_max: 'inf', step: 100}
  - j: {prefix: ' -j', type: float, min: 0, max: 10, default: 1, hard_min: 0, hard_max: 'inf', step: 10}
  - g: {prefix: ' -g', type: int, min: 0, max: 500, default: 50, hard_min: 0, hard_max: 'inf', step: 500}
  - M: {prefix: ' -M', type: float, min: 0, max: 1, default: 1, hard_min: 0, hard_max: 1, step: 10.0}
  - u: {prefix: ' -u', type: cag, usage: turn_on, min: 0, max: 1, default: 0, hard_min: 0, hard_max: 1, step: 1}
  - t: {prefix: ' -t', type: cag, usage: turn_on, min: 0, max: 1, default: 0, hard_min: 0, hard_max: 1, step: 1}
    

#working directory for eval software
gffcompare:
  directory: /data008/users/zyan/software/gffcompare/gffcompare
  command: ' -r '
gtfcuff:
  directory: /data008/users/zyan/software/rnaseqtools-1.0.3/gtfcuff/gtfcuff
  command: ' auc '